= form_for @story , :url => { :action=>"fairy_tales/create"} do |f|
  -if @story.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product.errors.count, "error")} prohibited this product from being saved:"
      %ul
        - @story.errors.full_messages.each do |msg|
          %li= msg
  = f.text_field :title,:id=>'cover_title'
  = f.text_field :sub_title,:id=>'cover_sub_title'
  #cover_part
    = f.label :part
    = f.text_field :part
  .actions
    = f.submit 'Save'





