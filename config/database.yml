# SQLite version 3.x
#   gem 'activerecord-jdbcsqlite3-adapter'
#
# Configure Using Gemfile
# gem 'activerecord-jdbcsqlite3-adapter'
#
development:
  adapter: mysql
  database: schatter_dev
  username: root
  password: 
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  adapter: mysql
  database: schatter_test
  username: root
  password: 
  host: localhost

production:
  adapter: mysql
  database: schatter_prod
  username: root
  password: 
  host: localhost

cucumber:
  <<: *test